# 使用说明

> A 股 Alpha158 + LightGBM 因子选股研究项目：数据下载 → Qlib bin 转换 → 模型训练 → 样本外回测（1 亿 / 10 万两版）→ 报告导出 → **每日建仓信号**。
> 实验结论与完整指标见 [RESULTS.md](RESULTS.md)；本文只讲"怎么用"和"怎么正确解读"。

## 1. 快速开始

```bash
brew install libomp        # macOS 上 LightGBM 运行时依赖(仅首次)
make setup                 # uv venv + 依赖安装 + import 自检
make data                  # 下载沪深300成分股+指数日线 → CSV → Qlib bin(约10分钟)
make train                 # 主实验: 1亿账户 训练+回测
make analyze               # 导出 IC/净值/回测 CSV 与图
```

关键版本：Python 3.11 · pyqlib 0.9.7 · lightgbm 4.7.0 · pandas 2.3.3 · akshare 1.18.94。
数据截止日固定在 Makefile `DATA_END ?= 20260821`，保证可复现。

## 2. 主实验（1 亿账户）

| 命令 | 作用 |
|---|---|
| `make train` | qrun 训练+样本外回测，日志写 `reports/qrun_output.log` |
| `make analyze` | 从实验 `workflow` 取最新 FINISHED recorder，导出全部产物 |

样本外区间 2025-07-01 ~ 2026-08-21（280 个交易日）。核心结果：RankIC +0.0147，策略累计 +6.62% vs 基准 +17.35%（超额 -9.14%）——**如实负结果**，详见 RESULTS.md。

## 3. 10 万元模拟盘

```bash
make train10w              # 独立实验 workflow_10w, 日志 reports/qrun_output_10w.log
make analyze10w            # 从 workflow_10w 导出(--experiment 参数化, 不污染主实验)
make signal                # 生成今日 Top-8 建仓清单 → reports/today_signal.txt
```

与主配置 `config/workflow_lightgbm.yaml` 的差异只有三处：账户 100000、topk 8、n_drop 2；
模型/特征/切分/成本完全一致，两版结果可直接对比。实验通过 yaml 顶层 `experiment_name: workflow_10w`
隔离（qlib run.py:145 原生支持），避免 recorder 互相混淆。

### 两版核心对比（280 日 OOS）

| 指标 | 1 亿版 | 10 万版 |
|---|---|---|
| 策略累计收益 | +6.62% | **+13.85%** |
| 超额累计(vs CSI300) | -9.14% | -2.98% |
| 策略最大回撤 | -7.74% | -7.92% |
| 日均换手率 | 20.0% | 47.6% |
| 总交易成本 | — | 22,589 元(初始资金的 22.6%) |
| 含成本年化超额 | -17.42%(IR -1.19) | -23.67%(IR -1.41) |
| 成交填充率 ffr | 1.000 | 1.000 |

一句话解读：10 万账户累计占优主要来自 topk8 集中度放大的单路径方差（噪声），
不是真实 alpha 增强；成本侵蚀在小账户上显著加重。

## 4. 每日信号（make signal）

```bash
make signal                                        # 默认日历最后交易日
.venv/bin/python scripts/today_signal.py --date 2026-08-21
```

输出内容：
- **Top-8 可建仓清单**：代码 / 预测分 / 真实收盘价 / 一手工需资金 / 预计占用金额
- **资金约束排除名单**：分数进前列但真实股价一手超预算（100,000÷8=12,500 元）的高分标的
- **停牌/无报价标的**单独列出

实现要点：
- 加载最新 FINISHED recorder 的 `params.pkl`（优先实验 `workflow_10w`，无则回退 `workflow`，
  与 Makefile a418da2 引入的防御逻辑同款）
- 推理特征**完整复现训练期 infer_processors**（RobustZScoreNorm clip_outlier + Fillna，
  fit 窗口锁训练段 2021~2024）；内置自检断言防止 fit 窗口未被数据覆盖导致的
  "特征全零、分数退化"静默错误
- 取数起点固定 2020-09-01：同时覆盖 Alpha158 最长回看窗与处理器 fit 窗

## 5. 每日自动模拟盘（paper trading）

```bash
.venv/bin/python scripts/paper_trade.py --init   # 首次: 初始化全部策略账户(各 ¥100,000)
make paper                                       # 收盘后运行: 抓数→双账户打分→模拟成交→记账
make paper-status                                # 只看双账户现状, 不入账
python scripts/paper_trade.py --replay 2025-07-01:2026-08-21   # A/B 对比回放自检(不落正式账本)
# 可选: --strategy ml_top8,dq_dvpb 过滤账户; --account N 自定资金
```

**A/B 双账户（t13 起）**：`make paper` 同时维护两个独立账本——

| 账户 | 策略 | 调度 | 账本文件 |
|---|---|---|---|
| ml | `ml_top8`: Alpha158+LightGBM 打分 | 每日(Top8进/跌出Top16出) | `paper/equity_ml.csv` 等 |
| dq | `dq_dvpb`: 股息率40%+低波30%+低PB30%(截面百分位加权) | **每月首个交易日**调仓, 其余日只记账 | `paper/equity_dq.csv` 等 |

dq 另受 **MA200 闸门**约束: 沪深300 收盘跌破 200 日线时, 新建仓目标市值减半
(注意: 闸门只节流新资金, 存量持仓不因此强制减仓)。dq 基本面因子数据来自
东方财富批量端点(`scripts/data_fundamentals.py`, parquet 缓存于 `paper/cache/`)——
原任务书指定的乐咕接口已被上游移除且端点需会员, 属被动替代, 因子口径不变。

每天回答一个问题：**今天赚没赚**。`make paper` 末尾分别打印两账户的今日盈亏 /
累计盈亏 / 同期沪深300 / 当前持仓明细；逐日记录在各自的 equity/trades 文件,
状态存 `state_ml.json` / `state_dq.json`(旧单账户文件已自动迁移为 *_ml)。

执行口径（与回测的差异在此声明）：
- T 日收盘后取数打分, 以 **T 日真实收盘价**（`$close/$factor`）成交——理想化假设,
  **无滑点模型**, 且比回测的"信号次 日成交"节奏更乐观;
- 成本与回测一致: 买 13bp / 卖 23bp / 单笔最低 5 元; 一手 100 股整数倍; risk_degree=0.95;
- 目标组合 = 分数 Top8(一手超预算者顺延补位); 持仓跌出分数 Top16 全部换出;
  已持有且仍在目标内的标的不做每日再平衡(避免小额订单被最低佣金持续侵蚀);
- 幂等: 同一交易日重跑会先回滚当日记录再重做, 绝不重复入账; 断档多日会自动补跑。

自动化（launchd）:
```bash
mkdir -p ~/Library/LaunchAgents paper && cp deploy/com.user.qlib-papertrade.plist ~/Library/LaunchAgents/
launchctl load ~/Library/LaunchAgents/com.user.qlib-papertrade.plist
```
周一~五 18:00 自动运行 `make paper`, 日志在 `paper/launchd.log`; **要求当日 Mac 开机**
(launchd 不补偿错过的触发)。

历史回放自检结论（2025-07-01~2026-08-21, 与 §3 回测同区间但为真实价整手口径,
完整数字见 `PAPER_LOG.md`）:

| 口径 | 累计 | 最大回撤 | 笔/日 | 成本 |
|---|---|---|---|---|
| ml 含费 | **-28.79%** | -33.02% | 7.1 | ¥30,771 |
| ml 零费用对照 | +2.87% | -11.29% | 同上 | ¥0 |
| dq(t13) | **-7.29%** | -18.91% | 0.1 | ¥653 |
| 基准沪深300 | +17.15% | — | — | — |

ml 的 **31.7pp 被交易成本吞噬**(¥30,771), 该弱信号策略在真实零售成本下不具备可
交易性; dq 凭月度低频把换手压到 ml 的约 1/70、成本拖累仅 6.5pp, 生存性显著更好,
但两策略均未跑赢同期基准——这正是模拟盘引擎要揭示的事实(参见 §6 第 2 条口径分裂说明)。

## 6. 【必读】口径与限制

1. **幸存者偏差**：股票池使用*当前*沪深300成分名单回测历史，结果系统性偏乐观（README 已知限制第 1 条）。本文所有负结果都是在偏乐观口径下得出的。
2. **可买性口径分裂（模拟盘参考边界）**——这是解读 10 万版结果前必须知道的：
   - 回测在 qlib 的**后复权价格空间**执行，"一手下限"作用在后复权价上：最新日复权价 >125 元的有 **148 只(49%)**，即回测实际运行在被动砍半的可买池上；
   - 每日信号按**真实价**（`$close/$factor`）判断可买性：真实价 >125 元的仅 **27 只(9%)**；
   - 两者分歧高达 **121 只**（后复权超限但真实世界完全可以买入，典型如中联重科复权 668 元 vs 真实 6.96 元）。
   - **结论**：10w 回测数字（+13.85% 等）是在与真实账户不同的可买集上得出的，**不能直接代表真实账户跟随每日信号的体验**，只适合做同口径的相对比较；工程修复（统一可买性空间）列为后续项。
3. **risk_degree=0.95**：TopkDropout 回测默认只投入账户的 95%，信号输出中已注明实际投入上限。
4. **reports/ 目录覆盖**：`make analyze` 与 `make analyze10w` 导出写同一目录、同名文件互相覆盖，需要并存时请先备份（目录级分离列为后续项）。
5. **mlflow 文件后端**：mlflow≥3 将本地文件存储设为维护模式，Makefile 已统一加 `MLFLOW_ALLOW_FILE_STORE=true` 放行，请勿去掉。
6. **数据尾部说明**：现有产物日历含 2026-08-24（比 DATA_END 多 1 个交易日，对测试段末端标签影响可忽略）；干净检出重跑以 `DATA_END ?= 20260821` 为准。

## 7. 常见问题

| 症状 | 原因与处理 |
|---|---|
| `import lightgbm` 失败 | macOS 缺系统级 OpenMP：`brew install libomp` |
| qrun 直接抛 MlflowException(maintenance mode) | 未走 Makefile，手动补 `MLFLOW_ALLOW_FILE_STORE=true` 前缀 |
| 训练"成功"但无 pred/IC/回测产物 | yaml 的 record 块未嵌在 task 下（qrun 只读 task.record），对照主配置检查 |
| make signal 分数全部相同 | 推理取数窗口未覆盖处理器 fit 窗口，脚本内自检断言会直接报错拦截 |
| analyze 提示无 recorder | 先跑对应 experiment 的训练：主实验 `make train`，10w `make train10w` |

## 8. 目录结构

```
├── config/workflow_lightgbm.yaml       # 主实验配置(1 亿账户)
├── config/workflow_lightgbm_10w.yaml   # 10万模拟盘配置(experiment_name=workflow_10w)
├── scripts/fetch_data.py               # 数据下载(东财→新浪→腾讯降级链)
├── scripts/dump_bin.py                 # CSV → Qlib bin
├── scripts/analyze_recorder.py         # 产物导出(--experiment 选实验)
├── scripts/today_signal.py             # 每日 Top-8 建仓信号
├── scripts/paper_trade.py              # 每日自动模拟盘引擎(--init/--status/--replay)
├── deploy/com.user.qlib-papertrade.plist  # launchd 自动运行模板(安装命令见文件头注释)
├── paper/                              # 模拟盘账本 state.json/equity.csv/trades.csv(gitignore)
├── data/csv_raw/                       # 原始日线 CSV(gitignore)
├── data/qlib_data/cn_data/             # Qlib bin(gitignore)
├── mlruns/                             # 实验跟踪(gitignore)
├── reports/                            # 导出报告(仅 ic_summary.txt / today_signal.txt 入库)
└── RESULTS.md                          # 实验结论与全指标
```

## 9. 结果速查

| 文件 | 内容 |
|---|---|
| `reports/ic_summary.txt` | IC/RankIC 汇总（两实验模型相同，数字一致） |
| `reports/equity_curve.png` | 策略 vs 基准累计净值曲线 |
| `reports/portfolio_analysis_port_analysis_1day.csv` | 年化收益/IR/最大回撤（含/不含成本） |
| `reports/today_signal.txt` | 最近一次每日信号 |
