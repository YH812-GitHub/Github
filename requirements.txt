# Qlib 0.9.7 提供 cp311 macOS universal2 轮子(Apple Silicon 可直接安装, 无需编译)
pyqlib==0.9.7

# 最新版 akshare 要求 Python >= 3.11; 锁死小版本以保证复现(接口易变动)
akshare==1.18.94

# qlib 0.9.7 的 Cython 扩展基于 numpy 1.x ABI 构建, 必须锁定 numpy<2
numpy==1.26.4

# 本仓库实测可用的组合(若后续训练报 pandas 兼容问题, 回退 pandas<2.2 重试)
pandas==2.3.3

lightgbm>=4.0
matplotlib>=3.8
tqdm>=4.66
fire>=0.6          # dump_bin.py 的 CLI 依赖
scipy>=1.11
pyarrow>=15        # 导出 parquet 用
